<%= form_for @post, html: { multipart: true  } do |f|  %>

	<%= f.label "image"   %>
	<%= f.file_field :image   %>
	

	<%= f.label "Title"   %>
	<%= f.text_field :title   %>
	<br>
	<%= f.label "Content"   %>
	<%= f.text_field :content   %>
	<br>
	<%= f.button :submit %>
<% end %>